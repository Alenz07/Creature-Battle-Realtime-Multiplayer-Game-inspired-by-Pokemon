I have used the proper MVC architecture so code will remain clean and understandable, app.js is the main file  then we have routes -------> pointing to the controller 
IN config database.js established connection to database
in models i have all my models and index.js shows how they are related used sequelize orm for ease alll 
all the frontend js part inside the public folder
html part inside the views. 
in app.js established all connections like used express, websockets etc.
in middleware i have the authentication part and socket handler 
seeder is a file where i have stored data of creatures which is preseeded in database
